import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  bordeless: {
    width: 56,
    height: 56,
  },
  container: {
    width: '100%',
    height: '100%',
    borderWidth: 1,
    borderColor: 'white',
    borderRadius: 8,
    alignItems: 'center',
    justifyContent: 'center',
  },
})
